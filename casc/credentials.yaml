credentials:
  system:
    domainCredentials:
      - credentials:
          - aws:
              id: aws-credentials
              scope: GLOBAL
              accessKey: ${AWS_ACCESS_KEY}
              secretKey: ${AWS_SECRET_KEY}
              description: "Credentials for AWS"